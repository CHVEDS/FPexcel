# Парсер Excel — зависимости
# Автор: Чураев Вадим Эдуардович || РЭУ ИМ.ПЛЕХАНОВА
#
# Установка:
#   pip install -r requirements.txt
#
# Примечание: xlwings требует установленного Microsoft Excel на Windows/macOS.

# --- Основные зависимости парсера ---
xlwings>=0.33.0          # Вычисление формул через Excel (включая русские функции, внешние ссылки)
openpyxl>=3.1.0          # Чтение/запись .xlsx: формулы, стили, изображения, объединённые ячейки
openpyxl-image-loader>=1.4.0  # Извлечение изображений, привязанных к ячейкам

# --- Изображения ---
Pillow>=10.0.0           # Сохранение/обработка извлечённых изображений
